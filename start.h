#include "utils.h"
#define OK 1

/******************************************************************************************************************************************************************/
/*A function that receives a pointer to the source file, a pointer to a newly created file (after the macros are removed) and the name of the source file (without suffix). The function executes the pre-assembler process*/
void pre_as (FILE*, FILE*, char*);
